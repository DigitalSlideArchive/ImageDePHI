<!DOCTYPE html>
<html lang ="en">
<head>
    <title>Select Directory</title>
</head>
<body>
<h2>
{% for item in bread_crumbs[1:] %}
    <a href="/?path={{ bread_crumbs[bread_crumbs.index(item)]|urlencode }}">{{ item.name }}</a>/
{% endfor %}
</h2>
    <form method="POST", action="/directory_selection/">
        <input type="submit", value="Choose Directory">
            <ul>
                {% for path in directories %}
                    <li style="list-style-type:none">
                    <input type="checkbox", id="{{ path }}", value="{{ path }}", name="directory">
                        <a href="/?path={{ path|urlencode }}">{{ path.name }}</a>
                    </li>
                {% endfor %}
            </ul>
    </form>
</body>
</html>
